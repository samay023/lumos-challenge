
import { Library } from '../src/library';

describe('Library', () => {
    let bank: Library;

    beforeEach(() => {
        // create a library
    });

    test('true to be true', () => {
        expect(true).toBe(true);
    });

});